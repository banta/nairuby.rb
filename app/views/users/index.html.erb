<% build_meta_tags title: "Ruby Users" %>
<h1>Ruby Users</h1>
<p>Here is a list of Rubyists (and some non-Rubyists) who have volunteered to talk about Ruby and Ruby development
  related topics in the monthly meetups:</p>
<ul class="user-list">
  <% @users.each do |user| %>
      <li>
        <strong>
          <%= link_to user.name, user %>
        </strong>
        <% unless user.presentations.count < 1 %>
            <%= pluralize user.presentations.count, "talk" %>
        <% end %>
      </li>
  <% end %>
</ul>
